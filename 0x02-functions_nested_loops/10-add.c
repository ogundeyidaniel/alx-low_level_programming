#include "main.h"

/**
 * add - A function that adds two integers and returns the result.
 * @q: number one.
 * @b: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	return (a + b);
}
